// types/profile.ts (Donor-specific data)

export interface DonorProfileData {
  street_address: string;
  city: string;
  state_region: string;
  about_you: string; 
  gov_id_url: string; 
  registration_cert_url:string;
}